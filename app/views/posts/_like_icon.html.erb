<% if is_liked.present? %>
  <%= link_to "Unlike", like_path(is_liked), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>
  <% else %>
  <%= link_to "Love", post_likes_path(post.id), method: :post, remote: true, class: "btn btn-sm btn-warning" %>
<% end %>
