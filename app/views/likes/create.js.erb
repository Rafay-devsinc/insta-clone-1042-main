$('#like-icon-post-<%= @post.id.to_s %>').html('<%= link_to "Unlike", like_path(@like), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>');

$('#like-text-post-<%= @post.id.to_s %>').html('<%= j render "posts/like_text", {post: @post} %>');
