$('#like-icon-post-<%= @post.id.to_s %>').html('<%= link_to "Love", post_likes_path(@post.id), method: :post, remote: true, class: "btn btn-sm btn-warning" %>');

$('#like-text-post-<%= @post.id.to_s %>').html('<%= j render "posts/like_text", {post: @post} %>');
