<h2>Search results</h2>
<table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">
    <tr>
      <th>User Name</th>
      <th>Email</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.name, user_path(user.id), method: :get %></td>
        <td><%= user.email %></td>
      </tr>
    <%end%>
  </tbody>
</table>
